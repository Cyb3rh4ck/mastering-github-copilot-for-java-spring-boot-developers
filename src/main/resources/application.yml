spring:
  application:
    name: clinicalsapi
  
  datasource:
    url: jdbc:mysql://localhost:3306/clinicals
    username: clinical_user
    password: Cl1n1c@ls!2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  
server:
  port: 8080
  servlet:
    context-path: /patientservices

logging:
  level:
    io.cyb3rh4ck.github.patienclinicals: DEBUG
    org.springframework.web: DEBUG